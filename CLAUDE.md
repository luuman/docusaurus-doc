# CLAUDE.md

本文为 Claude Code 提供项目指导信息。

## 命令

使用 **pnpm**（不是 yarn 或 npm）作为包管理器。需要 Node.js ≥ 18.0。

### 开发

```bash
pnpm start          # 启动开发服务器 http://localhost:3000/claude-doc/（中文）
pnpm start:en       # 启动英文环境的开发服务器
pnpm typecheck      # TypeScript 类型检查
```

### 构建与部署

```bash
pnpm build          # 构建静态网站到 build/ 目录
pnpm serve          # 本地预览构建后的网站
pnpm clear          # 清除缓存（构建行为异常时使用）
```

### 翻译

```bash
pnpm write-translations  # 提取新字符串用于翻译（添加新页面后执行）
```

## 项目架构

这是一个 **Docusaurus 3.9.2** 文档网站，主要使用中文编写（默认语言 `zh`），支持英文翻译（`en`）。

### 内容结构

- `docs/claude-code/` — 主要文档，按目录组织
- `_category_.json` 文件控制侧边栏标签、排序（`position`）和折叠行为
- 侧边栏从文件系统自动生成（`sidebars.ts` 使用 `autogenerated`）
- `docs/plans/` — 内部规划文档（不包含在发布的文档导航中）

### 国际化

- 默认语言：`zh`（中文），服务于 `/claude-doc/`
- 英文翻译：`i18n/en/docusaurus-plugin-content-docs/current/`
- UI 字符串：`i18n/{locale}/code.json` 和 `i18n/{locale}/docusaurus-theme-classic/`

### 自定义 UI

- `src/pages/index.tsx` — 首页
- `src/components/HomepageFeatures/` — 首页功能卡片
- `src/css/custom.css` — 全局 CSS 覆盖

### 配置文件

- `docusaurus.config.ts` — 网站配置、导航栏、页脚、Mermaid 支持
- `sidebars.ts` — 侧边栏配置（目前自动生成，无需手动编辑）

## MDX 注意事项

MDX 将 `<` 解析为 JSX 标签开启符。在 markdown 表格或文本中，始终转义裸露的 `<` 字符：

```
<!-- 错误 -->
| 遗漏率 | <5% |

<!-- 正确 -->
| 遗漏率 | &lt;5% |
```

这适用于任何 `<number`、`<variable` 或类似的非 JSX 模式。

## Mermaid 图表

Mermaid 通过 `@docusaurus/theme-mermaid` 启用。在 `.md`/`.mdx` 文件中使用 ` ```mermaid ` 代码块。

## 文档工作流

### 添加新文档

1. 在 `docs/claude-code/` 或其子目录中创建 `.md` 或 `.mdx` 文件
2. 在每个目录中创建 `_category_.json` 来控制侧边栏顺序和标签：
   ```json
   {
     "label": "您的部分",
     "position": 1
   }
   ```
3. 运行 `pnpm write-translations` 提取新内容用于英文翻译
4. 将英文翻译添加到 `i18n/en/docusaurus-plugin-content-docs/current/`
5. 对于 UI 字符串（导航栏、页脚等），更新 `i18n/{locale}/code.json`

### 关键文件

- `sidebars.ts` — 侧边栏配置（从文件系统自动生成；不要手动编辑）
- `docusaurus.config.ts` — 网站标题、URL、GitHub Pages 配置、Algolia 搜索
- `i18n/{locale}/docusaurus-theme-classic/` — Docusaurus UI 元素的翻译
- `src/pages/index.tsx` — 首页内容（修改登陆页时更新）
