---
summary: "`moltbot uninstall` 的 CLI 参考（移除网关服务 + 本地数据）"
read_when:
  - 您想要移除网关服务和/或本地状态
  - 您想要先进行试运行
---

# `moltbot uninstall`

卸载网关服务 + 本地数据（CLI 保持不变）。

```bash
moltbot uninstall
moltbot uninstall --all --yes
moltbot uninstall --dry-run
```

